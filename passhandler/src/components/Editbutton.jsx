import React from 'react';



const EditButton = ({ onClick }) => {
    return (
        
            <span className="edit-icon"><i onClick={() => { }} className="fas fa-copy cursor-pointer "></i></span>

    );
};

export default EditButton;
